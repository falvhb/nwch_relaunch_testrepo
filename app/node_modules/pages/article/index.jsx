import React from 'react';

import ArticleHeader from 'components/article-header/wrapper';
import ArticleContent from 'components/article-content/wrapper';

export default React.createClass({

  render() {
    return (
      <article itemScope itemType='http://schema.org/Article'>
        <ArticleHeader {...this.props.data} />
        <ArticleContent {...this.props.data} />
      </article>
    );
  }

});
