var _ = require('lodash');


var getWebsiteAdConfig = function (domain, adConfig) {
  var websiteConfigs = _.where(adConfig, {'websiteDomain': domain});
  if (websiteConfigs.length) {
    return websiteConfigs[0];
  }
};

module.exports = getWebsiteAdConfig;
