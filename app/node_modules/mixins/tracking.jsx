/* eslint-disable no-console */

export default {

  track(event, id, index) {
    console.info('-- track event --', this.constructor.displayName, event, '[path from page: ' + window.location.pathname + ']', index, id);
  }

};
