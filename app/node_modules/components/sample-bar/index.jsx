'use strict';

var React = require('react');

var SampleBar = React.createClass({

  render: function() {
    return (
      <div>
        <p>This is the sample bar component. Render me in an <code>iframe</code> later.</p>
      </div>
    );
  }

});

module.exports = SampleBar;
