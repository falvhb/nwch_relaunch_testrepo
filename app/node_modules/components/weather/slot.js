/* eslint-disable no-console */
var _ = require('lodash');

var parsedData = function(state) {
  console.info('============slot.js=============', state.api._data);
  var data = _.assign({}, {forecast: state.api._data.forecast});

  return data;

  // var forecast = state.api._data.weather.data.forecast;
  // var text = forecast.forecast.prog;

  // console.info("should be weatherText = ", text);
  // return {
  //   weatherText: _.get(text, 'prog')
  // };

};

module.exports = {
  iso: false,
  data: parsedData
};
