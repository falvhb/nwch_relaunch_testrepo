/* eslint-disable no-console */
var _ = require('lodash');


var parsedData = function(state) {
  console.info('============slot.js=============');
  var data = _.assign({}, {domain: state.api._data.domain});
  return data;
};

module.exports = {
  iso: false,
  data: parsedData
};
