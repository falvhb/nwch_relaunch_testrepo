import React from 'react';
import bem from 'react-bem-helper';

import Teaser from 'components/teaser';

const classes = new bem({
  name: 'recommendations-top',
  prefix: ''
});

export default React.createClass({

  propTypes: {
    teasers: React.PropTypes.array.isRequired
  },

  render() {

    return (

      <div {...classes()}>
        <h3 {...classes('title')}>Artikel zum Thema</h3>
        <div {...classes('grid')}>
          {this.props.teasers.map((teaser, index) => {
            return (<div {...classes('cell', 'inlined-right')} key={index}>
              <Teaser key={index} {...teaser} />
            </div>);
          })}
        </div>
      </div>

    );

  }

});
