var loadRessortNav = require('../../../../server/api/loadRessortNav');

module.exports = function(req, res) {
  loadRessortNav(req, res);

  // signal that we need to wait for the API to finish the request
  return true;
};
