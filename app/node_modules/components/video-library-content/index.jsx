import React from 'react';
import bem from 'react-bem-helper';

const classes = new bem({
  name: 'video-library-content',
  prefix: ''
});

export default React.createClass({

  displayName: 'VideoLibraryContent',

  propTypes: {
  },

  render() {
    return (
      <div {...classes('container')}>
        <p>Video component displaying the video selected goes in here.</p>
      </div>
    );
  }

});
