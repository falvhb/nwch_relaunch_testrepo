import React from 'react';
import bem from 'react-bem-helper';
import path from 'path';
import DomainVariations from 'config/domain-variations';

const classes = new bem({
  name: 'header-logo',
  prefix: ''
});

export default React.createClass({

  displayName: 'HeaderLogo',

  propTypes: {
    id: React.PropTypes.string.isRequired,
    mobile: React.PropTypes.bool
  },

  logoPath(id) {
    let key = this.props.mobile ? 'logoMobile' : 'logo';
    const prefix = process.env.STATIC_ASSETS || '/__node__/__static__/client';
    return path.join(prefix, 'images/logos', DomainVariations[id][key]);
  },

  render() {
    return (
      <a {...classes()} href='/' title='Startseite'>
        <img {...classes('img')} src={this.logoPath(this.props.id)} />
      </a>
    );
  }

});
