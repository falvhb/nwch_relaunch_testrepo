import React from 'react';
import _ from 'lodash';

import getAssetMeta from 'helpers/get-asset-meta';

export default Component =>  React.createClass({

  displayName: 'ImageGalleryWrapper',

  propTypes: {
    title: React.PropTypes.string,
    description: React.PropTypes.string,
    asset: React.PropTypes.object.isRequired
  },

  mappedData(props) {
    return _.assign({}, { images: props.asset.images }, getAssetMeta(this.props));
  },

  render() {
    return (
      <Component {...this.mappedData(this.props)} />
    );
  }
});
