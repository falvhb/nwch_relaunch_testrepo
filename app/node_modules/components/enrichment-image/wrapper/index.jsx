import React from 'react';

import EnrichmentImage from 'components/enrichment-image';
import DataMap from './data';

const Wrapper = React.createClass({

  render() {
    return (
      <EnrichmentImage {...this.props}/>
    );
  }

});

export default DataMap(Wrapper);
