import React from 'react';
import bem from 'react-bem-helper';

import ImageAsset from 'components/image-asset';

const classes = new bem({
  name: 'enrichment-image',
  prefix: ''
});

export default React.createClass({

  displayName: 'EnrichmentImage',

  propTypes: {
    asset: React.PropTypes.object,
    title: React.PropTypes.string,
    description: React.PropTypes.string,
    image_url: React.PropTypes.string,
    placement: React.PropTypes.string
  },

  render() {
    var { placement } = this.props;

    var isFloated = placement === 'left' || placement === 'right'
      ? true
      : false;

    return (
      <div {...classes(null, isFloated ? 'aside' : 'full')}>
        <ImageAsset
          {...this.props}
          size={isFloated ? 'small' : 'max'}
          ratio="16x9"
        />
      </div>
    );
  }

});
