import React from 'react';
import bem from 'react-bem-helper';

import Button from 'components/button';

const classes = new bem({
  name: 'video-library-search',
  prefix: ''
});

export default React.createClass({

  displayName: 'VideoLibrarySearch',

  propTypes: {
  },

  handleChange(event) {
    var value = event.target.value;
    this.props.changeSearchText(value);
  },

  render() {
    return (
      <div {...classes('container')}>
        <input {...classes('textfield')} value={this.props.searchText} onChange={this.handleChange} /><Button modifier="secondary" _event={this.search} label="Suchen" _target="#"/>
      </div>
    );
  }

});
