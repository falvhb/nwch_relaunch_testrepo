var helpers = require('helpers');

module.exports = function(state) {

  var parsedData = function(article) {
    return {
      intro: article.lead,
      mainAsset: helpers.getMainAsset(article.assets)
    };
  };

  return parsedData(state.article);

};
