import React from 'react';
import bem from 'react-bem-helper';

// import Icon from 'components/icon';

const classes = new bem({
  name: 'rule-cta',
  prefix: ''
});

export default React.createClass({

  displayName: 'RuleCTA',

  propTypes: {
    url: React.PropTypes.string,
    label: React.PropTypes.string,
    modifier: React.PropTypes.string,
    _event: React.PropTypes.oneOfType([
      React.PropTypes.func,
      React.PropTypes.string
    ])
  },

  render() {

    const { url, label, modifier, _event } = this.props;

    const labelEl = label ? <span {...classes('label')}>{ label }</span> : null;
    // const iconEl = icon ? <Icon id={ icon } {...classes('icon')} /> : null;
    const linkEl = url && label ?
      <a href={ url } {...classes('link')}>
        { labelEl }
      </a>
    : null;
    const buttonEl = _event && label ?
      <button {...classes('link')} onClick={_event}>
        { labelEl }
      </button>
    : null;

    return (
      <div {...classes(null, modifier)} >
        { linkEl || buttonEl || null }
        <div {...classes('rule')} />
      </div>
    );
  }

});
