import React from 'react';

export default React.createClass({

  propTypes: {
    content: React.PropTypes.string.isRequired
  },

  render() {
    return (
      <div className="copy" dangerouslySetInnerHTML={{ __html: this.props.content }} />
    );
  }

});
