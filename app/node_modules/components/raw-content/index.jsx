'use strict';

const React = require('react');

const RawContent = React.createClass({

  render() {
    return (
      <div className="copy" dangerouslySetInnerHTML={{ __html: this.props.content }} />
    );
  }

});

module.exports = RawContent;
