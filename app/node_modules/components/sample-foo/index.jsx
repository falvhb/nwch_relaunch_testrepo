'use strict';

var React = require('react');

var SampleFoo = React.createClass({

  render: function() {
    return (
      <div>
        <p>This is the sample foo component. Render me in an <code>iframe</code> later.</p>
      </div>
    );
  }

});

module.exports = SampleFoo;
