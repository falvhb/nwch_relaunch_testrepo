import React from 'react';
import bem from 'react-bem-helper';

import MainAsset from 'components/main-asset';
import Intro from 'components/intro';
import ArticleBody from 'components/article-body';
import Tags from 'components/tags';

const classes = new bem({
  name: 'article-content',
  prefix: ''
});

export default React.createClass({

  propTypes: {
    asset: React.PropTypes.element,
    intro: React.PropTypes.string,
    body: React.PropTypes.array.isRequired,
    tags: React.PropTypes.array
  },

  render() {
    return (
      <div {...classes()}>
        <MainAsset asset={this.props.asset} />
        <Intro text={this.props.intro} />
        <ArticleBody content={this.props.body} />
        <Tags items={this.props.tags} />
      </div>
    );
  }

});
