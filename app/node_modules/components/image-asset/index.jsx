import React from 'react';
import bem from 'react-bem-helper';

import Wrapper from './wrapper';

import ResponsiveImage from 'components/responsive-image';
import Caption from 'components/caption';

const classes = new bem({
  name: 'image-asset',
  prefix: ''
});

const ImageAsset = React.createClass({

  displayName: 'ImageAsset',

  propTypes: {
    image: React.PropTypes.object.isRequired,
    caption: React.PropTypes.object,
    microdata: React.PropTypes.object
  },

  render() {
    const { image, caption, microdata } = this.props;

    return (
      <figure {...classes()} {...microdata}>
        <ResponsiveImage {...image} />
        {caption
          ? <Caption {...caption} />
        :null}
      </figure>
    );
  }

});

export default Wrapper(ImageAsset);
