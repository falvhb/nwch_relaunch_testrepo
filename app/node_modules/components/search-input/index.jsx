import React from 'react';
import bem from 'react-bem-helper';

const classes = new bem({
  name: 'search-input',
  prefix: ''
});

export default React.createClass({

  displayName: 'SearchInput',

  render() {

    return (
      <div {...classes()}>
        <input type="text" name="input" {...classes('input')} placeholder="Suchen" />
      </div>
    );
  }

});
