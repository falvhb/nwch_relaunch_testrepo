import React from 'react';
import bem from 'react-bem-helper';

import getCaptionProps from 'helpers/get-caption-props';
import getEnrichmentPlacment from 'helpers/get-enrichment-placement';
import Caption from 'components/caption';

const classes = new bem({
  name: 'enrichment-snippet',
  prefix: ''
});

export default React.createClass({

  displayName: 'EnrichmentSnippet',

  propTypes: {
    asset: React.PropTypes.object,
    title: React.PropTypes.string,
    description: React.PropTypes.string,
    placement: React.PropTypes.string
  },

  render() {
    return (
      <div {...classes(null, getEnrichmentPlacment(this.props.placement))}>
        <div {...classes('pandoras-box')} dangerouslySetInnerHTML={{ __html: this.props.asset.html }} />
        <Caption {...getCaptionProps(this.props)} />
      </div>
    );
  }

});
