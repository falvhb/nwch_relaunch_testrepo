/* eslint-disable no-console */
var _ = require('lodash');
var mockData = require('components/video-library/.data.json');
// mockData = {mockData: 'empty'};


var parsedData = function(state) {
  var data = _.assign({}, mockData, state.api._data);
  console.info('---------------data from api:-------------', data);
  return data;
};

module.exports = {
  iso: true,
  data: parsedData
};
