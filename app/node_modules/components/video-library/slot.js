/* eslint-disable no-console */
var _ = require('lodash');


var parsedData = function(state) {
  console.info('============slot.js=============');
  var data = _.assign({}, state.api._data.videos, {domain: state.api._data.domain});
  return data;
};

module.exports = {
  iso: true,
  data: parsedData
};
