'use strict';

var React = require('react');

var Sample = React.createClass({

  render: function() {
    return (
      <div>
        <p>This is the sample component. Render me in an <code>iframe</code> later.</p>
      </div>
    );
  }

});

module.exports = Sample;
