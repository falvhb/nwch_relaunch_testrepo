import React from 'react';
import bem from 'react-bem-helper';

import VideoLibrarySearch from 'components/video-library-search';
import VideoLibraryFilter from 'components/video-library-filter';

const classes = new bem({
  name: 'video-library-header',
  prefix: ''
});

export default React.createClass({

  displayName: 'VideoLibraryHeader',

  propTypes: {
    changeSearchText: React.PropTypes.func.isRequired,
    onSearch: React.PropTypes.func.isRequired
  },

  render() {
    var title = 'Neuste Videos';

    return (
      <div {...classes('container')}>
        <h1 {...classes('title')}>{title}</h1>
        <VideoLibrarySearch {...classes('search')} changeSearchText={this.props.changeSearchText} onSearch={this.props.onSearch}/>
        <VideoLibraryFilter {...classes('filter')}/>
      </div>
    );
  }

});
