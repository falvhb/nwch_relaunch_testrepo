import ResponsiveImage from 'components/responsive-image';

import { expect } from 'chai';

const responsiveImage = new(ResponsiveImage);

describe('Responsive Image', function() {
  it('creates goldbach signature', function() {
    const signature = responsiveImage.createImageSignature({
      width: 100,
      height: 200,
      fill: true,
      salt: 'goldbach_7327'
    });
    expect(signature).to.equal('Xvab_iFD_lZ-FyfgR5fadpR7S9w/100x200,fill');
  });
});
