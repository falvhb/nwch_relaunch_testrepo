'use strict';

const React = require('react');

const ResponsiveImage = React.createClass({

  render() {
    return (
      <div>
        <img src={this.props.teaserImageFullSizeURL}/>
      </div>
    );
  }

});

module.exports = ResponsiveImage;
