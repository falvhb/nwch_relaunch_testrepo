'use strict';

const React = require('react');
const ReadMe = require('styleguide/readme');
const Viewer = require('styleguide/viewer');
const Variations = require('styleguide/variations');

var Component = React.createClass({

  propTypes: {
    readme: React.PropTypes.string,
    children: React.PropTypes.element
  },

  render() {

    var iframe = '/styleguide/component/' + this.props.component.slug + '/full';

    return (
      <div>
        <ReadMe {...this.props.component} />
        <Viewer src={iframe}/>
        <Variations variations={this.props.component.variations} />
      </div>
    );
  }

});

module.exports = Component;
