'use strict';

const React = require('react');
const bem = require('react-bem-helper');

var LayoutFull = React.createClass({

  propTypes: {
    children: React.PropTypes.element
  },

  render() {

    var classes = new bem({
      name: 'component-single',
      prefix: ''
    });

    return (
      <div {...classes()}>
        {this.props.children}
      </div>
    );

  }

});

module.exports = LayoutFull;
