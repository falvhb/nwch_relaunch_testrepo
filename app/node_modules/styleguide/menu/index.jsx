'use strict';

var React = require('react');
var MenuList = require('styleguide/menu-list');

var Menu = React.createClass({

  render() {
    return (
      <header className='styleguide-menu'>
        <a href='/styleguide' className={!this.props.active ? 'is-active' : ''}>Home</a>
        <nav role="navigation">
          <MenuList
            items={this.props.components}
            active={this.props.active}
            prefix='/styleguide/component/'
          />
        </nav>
      </header>
    );
  }

});

module.exports = Menu;
