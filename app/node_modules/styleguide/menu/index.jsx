'use strict';

var React = require('react');

var StyleguideMenu = React.createClass({

  componentDidMount() {
    // console.log(this.props.children);
  },

  menuItems() {
    var components = this.props.items;
    return components.map(function(component) {
      var title = component.componentName;
      var slug = component.componentSlug;
      return <li><a href={'/styleguide/component/' + slug}>{title}</a></li>;
    });
  },

  render() {
    return (
      <header className='styleguide-menu'>
        <nav role="navigation">
          <ul>
            {this.menuItems()}
          </ul>
        </nav>
      </header>
    );
  }

});

module.exports = StyleguideMenu;
