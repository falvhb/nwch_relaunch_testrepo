'use strict';

var React = require('react');

var Wrapper = React.createClass({

  render() {
    return (
      <main role='main' className='styleguide-wrapper'>
        {this.props.children}
      </main>
    );
  }

});

module.exports = Wrapper;
