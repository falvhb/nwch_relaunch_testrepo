import React from 'react';

export default React.createClass({

  displayName: 'SidebarToggle',

  toggleSidebar() {
    document.body.classList.toggle('is-sidebar-hidden');
  },

  render() {
    return (
      <button type='button' className='sidebar-toggle' onClick={this.toggleSidebar}>↔</button>
    );
  }

});
