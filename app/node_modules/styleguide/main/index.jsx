'use strict';

var React = require('react');

var Main = React.createClass({

  render() {
    return (
      <main role='main' className='styleguide-main'>
        {this.props.children}
      </main>
    );
  }

});

module.exports = Main;
