'use strict';

var React = require('react');
var Menu = require('styleguide/menu');
var Wrapper = require('styleguide/wrapper');
var Intro = require('styleguide/intro');

var Styleguide = React.createClass({

  getInitialState() {
    return {
      component: this.props.route
    };
  },

  render() {

    return (
      <div className='styleguide'>
        <Menu
          components={this.props.components}
          active={this.state.component}
        />
        <Wrapper>
          {this.props.children ? this.props.children : <Intro/>}
        </Wrapper>
      </div>
    );
  }

});

module.exports = Styleguide;
