import React from 'react';
import Markdown from 'react-remarkable';
import slugToTitle from 'slug-to-title';

import Container from 'components/container';

export default React.createClass({

  displayName: 'Readme',

  propTypes: {
    slug: React.PropTypes.string.isRequired,
    readme: React.PropTypes.string
  },

  render() {
    var content;

    if (this.props.readme) {
      content = (
        <div className="readme">
          <Markdown>{this.props.readme}</Markdown>
        </div>
      );
    } else {
      content = (
        <div className="readme">
          <h1>{slugToTitle(this.props.slug)}</h1>
          <p>You should add a <code>README.md</code> in the root of this component!</p>
        </div>
      );
    }

    return (
      <Container>
        {content}
      </Container>
    );
  }

});
