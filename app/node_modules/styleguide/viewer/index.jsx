'use strict';

const React = require('react');

var Viewer = React.createClass({

  render() {
    return (
      <iframe width='100%' src={this.props.src}>iFrame could not be displayed, sorry :(</iframe>
    );
  }

});

module.exports = Viewer;
