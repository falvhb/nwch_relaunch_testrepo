import React from 'react';
import { cut } from 'legacy/helpers/textfmt';

/*
<metal:macro define-macro="ArticleKeyword6Col">
  <div class="keyword"
       tal:content="textfmt:view/spitzmarke?cut=60:1:...">
       Keyword
  </div>
</metal:macro>
*/

export default class ArticleKeyword6Col extends React.Component {

  render() {
    const { view } = this.props;

    return (
      <div className="keyword">
        {cut(view.spitzmarke, { cutLen: 60, softcut: true, fill: '...' } )}
      </div>
    );
  }
}
