import React from 'react';
import { cut } from './legacy/helpers/textfmt.jsx';

//  <div class="keyword"
//       tal:content="textfmt:view/spitzmarke?cut=30:1:...">
//       Keyword
//  </div>

//        {view.spitzmarke.substring(0, 30)}
//        {cut(view.spitzmarke, { cutLen: 30, softcut: true, fill: '...' } )}

export default class ArticleKeyword3Col extends React.Component {

  render() {
    const { view } = this.props;

    return (
      <div className="keyword">
        {cut(view.spitzmarke, { cutLen: 30, softcut: true, fill: '...' } )}
      </div>
    );
  }
}
