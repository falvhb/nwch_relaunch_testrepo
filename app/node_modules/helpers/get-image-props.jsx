export default function(asset) {
  if (asset) {
    return {
      src: asset.image_url || asset.asset.image_url,
      title: asset.title || asset.asset.title,
      alt: asset.description || asset.asset.description
    };
  }
}
