import cookie from 'cookie';
import { get } from 'lodash/object';

export default function isLoggedIn() {
  var cookies = cookie.parse(document.cookie);
  var value = get(cookies, 'z3.authenticated', '').toLowerCase();

  return value === 'true';
}
