export default class NetMetrixTracker {
  constructor(options) {
    this.options = options;
  }

  getTag() {
    let tag = '@TODO: construct NetMetrix Tag';
    return tag;
  }

  track() {
    let image = new Image();
    image.src = this.getTag();

    return image.src;
  }

}
