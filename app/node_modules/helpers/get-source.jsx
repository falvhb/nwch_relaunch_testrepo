import capitalizeAll from 'helpers/capitalize-all';

export default function(source) {
  if (source) {
    let sourceString = source.title || (source.source ? source.source.title : '');
    return capitalizeAll(sourceString.replace('/', ' / ')) || '';
  }
}
