import _ from 'lodash';

const fallbacks = {
  'basic': 'light',
  'summary': 'light',
  'large': 'light'
};

export default function(variation) {
  return _.get(fallbacks, variation) || variation;
}
