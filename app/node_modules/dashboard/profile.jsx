import React from 'react';
import Container from 'dashboard/container';
import SectionHeader from 'components/section-header';

export default React.createClass({
  displayName: 'Profile',

  render() {
    return (
      <Container modifier="medium">
       <SectionHeader title='Profil'/>
      </Container>
    );
  }

});
