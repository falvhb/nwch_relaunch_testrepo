'use strict';

const React = require('react');

const ColorPalette = require('styleguide/color-palette');

const Colors = React.createClass({

  render: function() {

    return (
      <div>
        {this.props.sections.map((palette, index) =>
          <ColorPalette key={index} {...palette} />
        )}
      </div>
    );
  }

});

module.exports = Colors;
